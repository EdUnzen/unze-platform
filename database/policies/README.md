# RLS Policies

Alle aktiven Policies liegen in `../migrations/002_rls_policies.sql`.

Bei Schema-Änderungen:
1. Neue Migration anlegen (`004_*.sql`)
2. Policies dokumentieren
3. `npm run validate` ausführen
